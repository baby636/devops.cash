/* Import views. */
import CodeEditor from '@/views/Workspaces/CodeEditor'

/* Initialize routes. */
const routes = [{
    path: '/code-editor',
    component: CodeEditor
}]

/* Export routes. */
export default routes
